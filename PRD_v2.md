# PRD · AI PM 修炼场 v2.0

**文档状态**：v2.1 移动端适配版
**更新日期**：2026-06-05
**作者**：Hongzp
**技术栈**：HTML + CSS + JavaScript + DeepSeek API

---

## 一、版本变更说明（vs v1.0）

### 1.1 新增的核心改进方向（6 项）

| 编号 | 改进点 | 价值 |
|------|--------|------|
| ① | **AI 分层对话记忆** | 解决长对话 Token 爆炸；让 AI 真正"记住"用户 |
| ② | **结构化成长报告** | 周/月度自动汇总，可导出 PDF 形成学习闭环 |
| ③ | **AI 反向追问模式** | 训练 PM "先问清楚再动手" 的核心思维方式 |
| ④ | **知识图谱掌握进度** | 已掌握/学习中/待学习个性化标记 |
| ⑤ | **面试题难度分级** | 入门/进阶/资深三档，AI 推荐挑战 |
| ⑥ | **每日一问历史日历** | 月历视图回顾过去答题记录 |
| ⑦ | **Cmd+K 全局搜索** | 跨术语/知识/题库统一检索 |

### 1.2 砍掉的方向

- ✗ **"AI 工具输出对比（三方案并列）"** —— 重复造轮子，Skill Prompt 已足够；3 倍 Token 消耗；用户认知负担大；偏离 PM 核心能力培养

---

## 二、AI 分层对话记忆系统【重点章节】

### 2.1 设计目标

解决三个核心问题：

1. **Token 爆炸**：12 轮对话全量传递可达 8000+ tokens，单次对话 ¥0.05+
2. **跨会话失忆**：用户每次都要重复说明背景、偏好
3. **AI 缺乏个性化**：所有用户都得到一样的回答，没有"记住"用户

### 2.2 三层记忆架构

```
┌─────────────────────────────────────────────┐
│  长期记忆（用户画像层）                       │
│  跨所有会话 · 持久化 · 仅 20-50 tokens        │
│  例：[偏好:To B / 强项:用研 / 弱项:数据指标]  │
└─────────────────────────────────────────────┘
              ↓ 注入到每次请求
┌─────────────────────────────────────────────┐
│  中期记忆（摘要层）                           │
│  当前会话早期对话 · AI 压缩 · 约 150-300 tokens│
│  例：摘要：用户先后讨论了X、Y，倾向Z观点      │
└─────────────────────────────────────────────┘
              ↓ 注入到每次请求
┌─────────────────────────────────────────────┐
│  短期记忆（原文层）                           │
│  最近 3-4 轮原文对话 · 完整保留               │
│  约 200-500 tokens                          │
└─────────────────────────────────────────────┘
```

### 2.3 数据结构设计

```javascript
// 用户画像（localStorage: user_{u}_profile）
{
  preferences: ["To B", "AI 产品"],        // 偏好领域
  strengths: ["用户研究", "竞品分析"],      // 强项（基于答题表现）
  weaknesses: ["数据指标设计", "商业化"],   // 弱项
  conversation_count: 12,                  // 累计会话数
  last_updated: "2026-06-04T14:32:00Z"
}

// 单次会话（localStorage: user_{u}_session_{id}）
{
  id: "sess_abc123",
  title: "AI产品PMF评估",
  category: "AI产品",
  created_at: "...",
  rounds: [
    { role: "ai", content: "..." },        // 完整保留
    { role: "user", content: "..." },
  ],
  summary: "前 4 轮摘要：用户讨论了...",   // 当 rounds > 6 时生成
  summary_round_count: 4,                  // 已被摘要的轮次数
  final_score: 87,                         // 完成后的评分
}

// 会话索引（localStorage: user_{u}_sessions_index）
[
  { id: "sess_abc", title: "...", date: "...", score: 87, category: "AI产品" },
  ...最近 50 条
]
```

### 2.4 Token 节省的 5 个机制

#### 机制 1：滑动窗口 + 自动摘要

```
触发条件：当对话轮数 > 6
执行流程：
  1. 取出第 1 ~ N-3 轮（保留最后 3 轮原文）
  2. 调用 AI（用便宜的小模型）压缩为 200 字摘要
  3. 后续请求中只发送：摘要 + 最后 3 轮原文

效果：
  - 12 轮对话：8000 tokens → 700 tokens（节省 91%）
  - 6 轮以内：不触发摘要，正常传递
```

#### 机制 2：用户画像替代历史原文

```
不传："你上次说想做面向中小企业的 SaaS 产品，
       关注 LTV 和留存..." （200+ tokens）

而传："[用户画像: 偏好 To B SaaS / 关注指标: LTV+留存]"
      （30 tokens）

Token 节省：85%
```

#### 机制 3：本地索引 + 按需召回

```
策略：
- 历史会话默认不加载，只存本地
- 当用户提问包含"上次/之前/那个"等关键词时，
  本地全文检索 → 找到相关会话 → 只注入相关片段摘要

技术实现：
- localStorage 存所有会话
- 用 RegExp 检索关键词命中的会话
- 注入格式：[相关历史: {会话标题} - {200字摘要}]
```

#### 机制 4：智能预算控制

```
配置：
- 每次请求总 Token 预算：1500
- 优先级分配：
  1. System Prompt (固定 200)
  2. 当前问题 (动态，预留 300)
  3. 用户画像 (固定 50)
  4. 短期记忆（最近原文）(动态，最多 500)
  5. 中期记忆（摘要）(动态，最多 300)
  6. 跨会话引用（按需）(最多 150)

超出预算时：从优先级低的开始裁剪
```

#### 机制 5：用户可控开关

```
设置面板提供三档：
- 🟢 经济模式：仅用户画像 + 最近 2 轮原文 (~300 tokens)
- 🔵 平衡模式：画像 + 摘要 + 最近 3 轮 (~500 tokens) [默认]
- 🟣 完整模式：画像 + 全摘要 + 最近 6 轮 (~900 tokens)
```

### 2.5 用户画像自动提炼机制

```
触发时机：
- 每次会话结束（用户主动结束 或 完成评分）
- 每周日凌晨自动批处理

提炼流程：
  1. 收集本周所有会话的摘要 + 评分
  2. 调用 AI 分析：
     - 哪些领域出现频率高？→ 偏好
     - 哪些题目得分>85? → 强项
     - 哪些题目得分<70? 哪些被反复追问? → 弱项
  3. 生成新画像 → 与旧画像合并去重
  4. 写回 localStorage
  5. 通知用户：「你的画像已更新」
```

### 2.6 透明化 UI 设计

每次对话顶部显示：

```
┌─────────────────────────────────────────────┐
│ 🧠 已注入记忆：用户画像 + 1-4 轮摘要 + 最近 3 轮原文 │
│ 📊 预计本轮消耗 380 tokens（对比全量节省 71%）      │
│ [展开详情] [调整记忆策略]                          │
└─────────────────────────────────────────────┘
```

侧边面板显示 Token 消耗分解：
- 用户画像：28 tokens
- 早期对话摘要：180 tokens
- 最近 3 轮原文：172 tokens
- 总计：380 tokens
- 对比全量：1320 tokens（节省 71% / 约 ¥0.012）

### 2.7 边界处理

| 场景 | 处理 |
|------|------|
| 用户切换账号 | 各用户记忆完全隔离（localStorage 前缀） |
| 清空浏览器数据 | 提示用户「记忆将清除，可先导出」 |
| API 失败 | 降级为无记忆模式，仅传当前问题 |
| 用户主动关闭记忆 | 设置中提供总开关 |
| 摘要生成失败 | 静默降级为最近 3 轮原文 |
| 多设备同步 | v1 不支持（数据本地化）；v2 考虑 |

### 2.8 实际 Token 节省效果对比

| 场景 | 全量传递 | 分层记忆 | 节省 |
|------|---------|---------|------|
| 3 轮对话 | 380 | 380 | 0%（不触发） |
| 6 轮对话 | 1240 | 580 | 53% |
| 10 轮对话 | 2680 | 720 | 73% |
| 20 轮对话 | 5800 | 850 | 85% |
| 跨 5 个会话（含画像） | 12000+ | 950 | 92% |

---

## 三、AI 反向追问模式【重点章节】

### 3.1 设计动机

**问题**：用户输入需求 → AI 直接生成方案 → 这只是「使用 AI」，不是「学习 PM」

**目标**：让 AI 像一个 PM 导师，引导用户**主动思考**，而非被动接受。

### 3.2 触发场景

适用于所有"创造类"AI 工具：
- ✅ PRD 生成器
- ✅ 市场调研助手
- ✅ 竞品分析器

### 3.3 交互流程

```
用户输入需求 → AI 不立即生成
              ↓
[反向追问 3 个关键问题]
  1. 目标用户画像（是谁、什么场景、什么痛点）
  2. 核心价值/差异化（凭什么赢过竞品）
  3. 商业模式（如何变现、单元经济模型）
              ↓
用户逐一回答（每个问题可"跳过/请 AI 帮我想"）
              ↓
[AI 综合所有输入] → 生成最终方案
              ↓
方案末尾附：「你的回答已被纳入，这就是为什么方案聚焦于 XX」
```

### 3.4 关键体验点

1. **不强制**：用户可点"跳过追问直接生成"（但有提示这不是最佳实践）
2. **可代答**：每个问题提供"请 AI 帮我想"，AI 给出参考答案让用户选
3. **可见进度**：右侧面板显示 1/3、2/3、3/3 的清晰进度
4. **教学价值**：每个问题旁附小贴士「为什么这个问题重要」

### 3.5 Skill Prompt 设计

```
System: 你是一位资深 PM 导师，正在通过苏格拉底式追问帮助用户
理清产品思路。

规则：
1. 在用户给出初始需求后，必须先提出 3 个关键澄清问题
2. 每个问题聚焦一个维度：用户/价值/商业模式
3. 问题要具体、可回答，避免"你怎么想"这类空洞问题
4. 用户回答后，简短肯定并自然过渡到下一个问题
5. 三个问题都回答后，才生成完整方案
6. 方案末尾要明确指出"你的XX回答指引我聚焦于YY"

用户初始需求：{userInput}
```

### 3.6 数据追踪

每次完整经历追问流程的用户，标记：
- `tracks.reverse_question_complete += 1`
- 后续可解锁成就「先问后做」

---

## 四、其他改进方向（简述）

### 4.1 结构化成长报告

- 周报 & 月报，每周日自动生成
- 包含：成长指数、亮点、能力维度增长、AI 学习建议、活跃热力图
- 导出 PDF 功能（用 html2pdf 库）
- 可分享链接（URL hash 编码）

### 4.2 知识图谱掌握进度

- 每个节点 3 种状态：✓ 已掌握 / ◐ 学习中 / ○ 待学习
- 用户手动标记 / 浏览触发自动标记
- 按 8 大能力域显示掌握进度条
- AI 推荐"下一步学什么"

### 4.3 面试题难度分级

- 28 题按 3 档分级：🟢 入门 (12) / 🟡 进阶 (11) / 🔴 资深 (5)
- 解锁机制：完成 5 道入门后解锁进阶；完成 8 道进阶后解锁资深
- AI 推荐挑战题（基于用户已完成情况 + 弱项）

### 4.4 每日一问历史日历

- 月历视图，每个日期显示当天得分
- 点击日期查看历史答题详情 + AI 点评
- 支持月份切换、跨月统计
- 显示连续答题天数、平均分

### 4.5 Cmd+K 全局搜索

- 快捷键唤起（Cmd/Ctrl + K）
- 跨术语库、知识图谱、面试题库、历史会话统一搜索
- 键盘导航（↑↓ ↵ Esc）
- 显示分组结果（术语 / 知识点 / 面试题 / 历史记录）

---

## 五、技术实现要点

### 5.1 新增依赖
- `marked.js` （已有）
- `html2pdf.js` （新增，用于成长报告 PDF 导出）

### 5.2 新增模块文件

```
components/
  ├── memory.jsx        # 分层记忆系统（核心）
  ├── reverse-ai.jsx    # AI 反向追问模式
  ├── report.jsx        # 成长报告
  ├── search-palette.jsx # Cmd+K 全局搜索
  └── ... (其他已有)
```

### 5.3 LocalStorage 数据扩展

```
user_{u}_profile         # 用户画像（新增）
user_{u}_session_{id}    # 单次会话详情（新增）
user_{u}_sessions_index  # 会话索引（新增）
user_{u}_kg_mastery      # 知识图谱掌握状态（新增）
user_{u}_daily_history   # 每日一问历史（新增）
user_{u}_token_log       # Token 消耗日志（新增）
user_{u}_settings        # 用户设置（含记忆策略）（新增）
```

### 5.4 开发优先级

| 优先级 | 模块 | 工作量 |
|--------|------|--------|
| P0 | 分层记忆系统 | ⭐⭐⭐⭐ |
| P0 | AI 反向追问 | ⭐⭐ |
| P1 | 知识图谱掌握进度 | ⭐⭐ |
| P1 | 面试题难度分级 | ⭐⭐ |
| P2 | 成长报告 | ⭐⭐⭐ |
| P2 | 每日一问历史日历 | ⭐⭐ |
| P3 | Cmd+K 全局搜索 | ⭐⭐⭐ |

---

## 六、关键设计原则

1. **节省优先于强大**：所有 AI 功能必须考虑 Token 成本
2. **透明可控**：用户能看到 AI 用了什么记忆、消耗多少 Token
3. **教学优于使用**：宁可让用户多思考一步，也不直接给答案
4. **本地优于云端**：所有数据本地存储，保护隐私
5. **渐进披露**：复杂功能隐藏在默认配置后，进阶用户可调整

---

## 七、待用户确认事项

1. ✅ 砍掉 "AI 工具输出对比"
2. ✅ 新增 "AI 反向追问"
3. ✅ 分层记忆系统的 5 大节省机制
4. 待确认：开发优先级（P0 是否先做？）
5. 待确认：是否需要"导出/导入记忆"功能（便于多设备）

---

## 八、移动端适配【v2.1 新增】

### 8.1 设计目标

让整套平台在手机浏览器上可用，**且完全不影响电脑端体验**——桌面端代码路径零改动，移动端仅做条件覆盖。

### 8.2 断点与总策略

| 项 | 取值 |
|----|------|
| 移动端断点 | `≤ 768px`（`useIsMobile()` / `useMediaQuery` 钩子，监听 `matchMedia`） |
| 平板 | 不单独处理，归入桌面或移动 |
| 实现方式 | 全部 inline style 无法用媒体查询 → 用 JS 断点钩子返回 `isMobile`，按需切换样式分支；桌面分支保持原样 |
| 全局 CSS | 仅新增一个 `@media (max-width:768px)` 块，处理 inline style 管不到的事 |

### 8.3 框架层改造

| 模块 | 桌面端 | 移动端 |
|------|--------|--------|
| 导航 | 固定侧边栏（220/64px，可折叠） | **汉堡抽屉**：顶部栏汉堡键唤起，左滑出 270px + 半透明遮罩，点导航自动收起 |
| 顶部栏 | 无 | 新增 `MobileTopBar`（汉堡 / Logo / 搜索图标），sticky、含刘海安全区 |
| Modal（设置等） | 居中弹窗 | **底部上滑 sheet**，圆角顶、贴底安全区 |
| Toast | 右上角 | 顶部通栏居中 |
| Cmd+K 搜索 | 居中浮层 + 底部快捷键提示 | 近全屏 sheet，隐藏快捷键脚注 |

### 8.4 页面层改造（栅格降级 + 面板改造）

| 页面 | 改造 |
|------|------|
| 知识图谱 | 手机端**思维导图 → 可折叠列表视图**（按能力域分组手风琴，点知识点进详情）；桌面端仍是思维导图。两者数据互通 |
| 知识点 / 词条详情 | 右侧抽屉 → 底部上滑 sheet（顶部留 12%） |
| 模拟面试 chat | 气泡 78%→88%；输入框吸底 + 键盘安全区；记忆面板默认收起，展开为全屏覆盖 |
| 首页 / 仪表盘 / 报告 | 4 列 stats → 2 列；多列区 → 单列；雷达图+成就 1fr1fr → 单列 |
| 面试题库 / 名词百科 / AI 反转 | 多列卡片网格 → 单列；左右分栏 → 上下堆叠 |
| 成长报告 | 头图横排 → 竖排；周/月报控件自适应 |
| 登录 / 注册 | 卡片 padding 收窄，自适应窄屏 |

### 8.5 触控与输入细节

- 输入框统一 `font-size:16px`，避免 iOS 聚焦自动缩放
- `viewport-fit=cover` + `env(safe-area-inset-*)` 适配刘海/底部条
- 移动端放开 `body { overflow }` 锁，允许页面滚动（键盘弹出不顶飞布局）
- 关闭 hover tooltip（触屏无 hover）、去除 tap 高亮、内层滚动加惯性
- Badge / Button 文案强制 `nowrap`，避免窄屏折行

### 8.6 数据存储说明（移动端同源）

- 手机端数据同样存于**该浏览器的 localStorage / sessionStorage**，纯本地、不上云、**不跨设备同步**
- 同一手机换浏览器（Safari↔Chrome）也互不相通；跨设备迁移需用「设置 → 数据管理 → 导出/导入 JSON」
- 清理浏览器数据 = 全部丢失（建议在 UI 中向用户提示）

### 8.7 验收记录（v2.1）

- ✅ 桌面端回归：侧栏 220px、思维导图、右侧面板均无变化
- ✅ 移动端逐页渲染：首页 / 知识列表 / 详情 sheet / 仪表盘 / 面试题库 / chat 会话 / 词条 sheet / 报告
- ✅ 功能流程：注册→自动登录、登出→登录、Cmd+K 搜索、设置弹窗、生词收藏、每日一问提交（无 Key 正确告警）、PDF 导出触发无报错
- ⏳ 待你自测：真实 DeepSeek AI 生成链路（市调/竞品/PRD/面试追问/评分），需配置 API Key
