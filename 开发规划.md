# AI PM 修炼场 — 完整开发规划

## 当前状态
✅ UI 原型已完成（视觉方向已确认）
❌ AI 功能为模拟数据，需接入真实 DeepSeek API

---

## 阶段一：核心基础设施（API + 数据层）

### 1.1 DeepSeek API 流式调用封装
- 封装统一的 `callDeepSeek(messages, onChunk, onDone, onError)` 函数
- 实现 SSE 流式读取（`stream: true`）
- Token 实时拼接 + 回调输出
- 完整错误处理：无 Key / 4xx / 5xx / 网络断开
- API 端点：`https://api.deepseek.com/v1/chat/completions`

### 1.2 marked.js 集成
- 引入 `marked.js@9.1.6` CDN
- 替换当前简易 Markdown 渲染器为 marked.js
- 支持表格、代码块、列表、引用等完整 Markdown 语法
- 添加代码高亮的暗色主题样式

### 1.3 数据追踪系统
- 实现 `trackActivity(user, action)` 全局追踪函数
- 所有模块调用时自动记录到 `user_{u}_track`（最近 40 条）
- 实现 `updateStats(user, dimension)` 统计更新函数
- 成就系统触发检测：每次操作后检查是否解锁新成就

---

## 阶段二：AI 工具箱（3 个核心工具）

### 2.1 市场调研助手
- 内置完整 Skill Prompt（战略顾问视角 + 12 章节分析框架）
- 输出结构：12 章节报告 + 市场机会综合评分 + A/B/C 可信度分级
- 流式输出 + marked.js 渲染

### 2.2 竞品分析器
- 内置完整 Skill Prompt（竞争情报视角 + 6 步推理链）
- 输出结构：12 章节报告 + 分角色行动建议
- 流式输出 + marked.js 渲染

### 2.3 PRD 生成器
- 内置完整 Skill Prompt（七要素解析 + 三模式自适应）
- 输出结构：完整 PRD（含流程图描述 / 验收标准）
- 流式输出 + marked.js 渲染

### 2.4 导出功能
- ✅ 复制 Markdown 到剪贴板（已完成）
- ✅ 下载 .md 文件（已完成）

---

## 阶段三：知识体系 + 名词百科

### 3.1 知识图谱 AI 解析
- 点击子节点 → 调用 DeepSeek 生成深度解释
- System Prompt：PM 知识导师角色，结合实例讲解
- 流式输出到侧边面板
- 每次查看计入 stats.knowledge

### 3.2 名词百科 AI 大白话解释
- 点击词条 → 调用 DeepSeek 生成通俗解释
- System Prompt：用大白话 + 类比 + 实战场景解释 PM 术语
- 每次查阅计入 stats.dictionary

### 3.3 生词本功能
- ✅ 收藏/取消收藏（已完成）
- ✅ 筛选「生词本」Tab（已完成）

---

## 阶段四：面试 + 每日一问

### 4.1 模拟面试官
- 题库扩充至 28 题（按 PRD 要求），覆盖 6+ 分类
- AI 面试官 System Prompt：
  - 前 3 轮：只追问不给答案，每轮 1-2 个深入问题
  - 第 3 轮后：输出「点评」含优点 2 条 + 改进点 2 条 + 参考框架
- 完整对话历史传递（多轮 context）
- 「换题」按钮重置对话
- 每次完成计入 stats.interview

### 4.2 每日一问
- 按日期循环出题（保持当前逻辑）
- 提交后调用 DeepSeek 进行评分 + 解析
- 每日仅可提交一次（`user_{u}_daily_{date}` 标记）
- 「查看提示」功能保留
- 完成后计入 stats

---

## 阶段五：成长仪表盘增强

### 5.1 实时统计
- 4 维统计数据实时读取（知识/工具/词典/面试）
- 所有模块操作自动触发统计更新

### 5.2 能力雷达图
- ✅ Canvas 绘制（已完成）
- 数据源改为基于真实使用统计计算

### 5.3 成就系统
- 7 个勋章的完整触发条件实现：
  - 初入江湖：首次登录 ✅
  - 工具达人：首次使用 AI 工具
  - 求知若渴：收藏 5 个生词
  - 面霸初成：完成 3 次模拟面试
  - 持之以恒：连续 7 天每日一问
  - 全知全能：浏览全部知识节点
  - 效率大师：累计使用 AI 工具 10 次

### 5.4 学习轨迹
- 自动记录所有学习行为（最近 40 条）
- 时间戳 + 行为描述

---

## 阶段六：打磨与测试

### 6.1 异常处理
- 未设置 API Key → 友好提示卡片 + 设置入口
- API 请求失败 → 具体错误信息展示
- 输入为空 → 阻断提交
- 词典无匹配 → 空状态提示
- marked.js 加载失败 → 降级为纯文本

### 6.2 交互打磨
- 流式输出的流光游标动效
- 页面切换过渡动画
- 按钮/卡片 hover 反馈
- Loading 状态统一

### 6.3 数据隔离验证
- 多用户注册/切换测试
- 确认 LocalStorage 前缀隔离

---

## 文件结构规划

```
ai-pm-arena/
├── index.html              ← 入口（CSS + 脚本引用）
├── components/
│   ├── ui.jsx              ← 主题 + UI 基础组件
│   ├── api.jsx             ← 【新增】DeepSeek API 封装 + 数据追踪
│   ├── prompts.jsx         ← 【新增】3 个工具的 Skill Prompt 定义
│   ├── login.jsx           ← 登录/注册
│   ├── knowledge.jsx       ← 知识图谱（接入真实 API）
│   ├── tools.jsx           ← AI 工具箱（接入真实 API）
│   └── growth.jsx          ← 面试 + 每日一问 + 仪表盘（接入真实 API）
└── app.jsx                 ← 主应用框架
```

---

## 预估工作量
| 阶段 | 内容 | 复杂度 |
|------|------|--------|
| 一 | API + 数据层 | ⭐⭐ |
| 二 | AI 工具箱 | ⭐⭐⭐ |
| 三 | 知识 + 百科 | ⭐⭐ |
| 四 | 面试 + 每日一问 | ⭐⭐⭐ |
| 五 | 成长仪表盘 | ⭐⭐ |
| 六 | 打磨测试 | ⭐⭐ |
